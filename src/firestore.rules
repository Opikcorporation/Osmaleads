rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ATTENTION : Règles de développement. Autorise toutes les lectures et écritures.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
