rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ATTENTION : Ces règles sont complètement ouvertes pour le développement.
    // La sécurité devra être réimplémentée avant la mise en production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
